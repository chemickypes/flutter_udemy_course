

import 'package:flutter/material.dart';

class SpacedBox25 extends StatelessWidget {
  const SpacedBox25({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: SizedBox(
            height: 25.0,
          ),
    );
  }
}